-- =====================================================================
-- 0005_session_snapshot.sql
-- 대진표 공유/표시용 스냅샷 컬럼.
-- 정규화된 matches 테이블은 스코어/통계용 RAW 데이터로 유지하고,
-- 게스트 공유 화면은 생성 당시 모습 그대로(휴식/시간/특이사항 포함)를 보여주기 위해
-- generateDraw 결과 전체를 JSON 으로 함께 저장한다.
-- =====================================================================

alter table sessions add column if not exists draw_json jsonb;
